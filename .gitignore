# ───────────────────────── OS junk ─────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ───────────────── Node / package managers ─────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.pnpm-store/
# lockfiles ARE committed on purpose (reproducible installs) — do NOT ignore them

# ──────────────── Test artifacts (Playwright) ───────────────
test-results/
playwright-report/
blob-report/
playwright/.cache/
.lighthouseci/

# ──────────────────── Build / cache output ─────────────────
dist/
build/
.cache/
.parcel-cache/
.vite/

# ─────────── Secrets & env (defensive — none today) ─────────
# Never commit credentials. contact.json holds PUBLIC site contact info — it is tracked on purpose.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa*
id_ed25519*
secrets.json
credentials.json
service-account*.json
.netrc

# ───────────── Third-party vendored repo (reference only) ──────────────
# jezweb-claude-skills is an external repo with its own .git/LICENSE, copied
# into an old design variant. Kept on disk as reference; not part of our repo.
old versions - tests/variant-f-design-loop/skills/jezweb-claude-skills/

# ───────────── Camera RAW masters (not web assets) ─────────────
# ~51MB of .cr3 lecture sources. Backed up in iCloud; would only bloat the repo
# and GitHub Pages without speeding up the site (visitors never download them).
*.cr3
*.CR3
*.cr2
*.arw
*.nef
*.dng

# ──────────────────────── Editor ──────────────────────────
.idea/
*.swp
*.swo
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
